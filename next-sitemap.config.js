module.exports = {
    siteUrl: process.env.SITE_URL || "https://www.rabihutomo.com",
    generateRobotsTxt: true, // (optional) Generate `robots.txt` alongside your `sitemap.xml`
    // ...other options
};
